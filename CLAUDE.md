# ESP2068 — TS2068 port of ESPectrum

## What this project is

A Timex Sinclair 2068 emulator, built by porting [EremusOne/ESPectrum](https://github.com/EremusOne/ESPectrum) (an ESP32 + VGA + PS/2 ZX Spectrum 48K/128K/Pentagon emulator) to run the TS2068 on the same ESP32 hardware. The 2068's SCLD chip replaces the Sinclair ULA; everything else (Z80 core, VGA DMA driver, AY sound, PS/2 keyboard, SD/file-nav, OSD menu) is reused as a chassis.

The technical design — SCLD register contract, 8-slot memory model, display modes, timing, cartridge loading, and the four-slice work breakdown — lives in [TS2068-ESPECTRUM-PORT-PLAN.md](TS2068-ESPECTRUM-PORT-PLAN.md). Treat that file as the spec every slice codes against. The phased build-order plan is in [PLAN.md](PLAN.md).

## Current repo state

ESPectrum's full source and history are vendored in (merged from the `upstream` remote, `EremusOne/ESPectrum`, `master` branch — see Phase 0 in `PLAN.md`). `src/`, `include/`, `platformio.ini` etc. now exist at repo root alongside the planning docs. To pull upstream fixes later: `git fetch upstream && git merge upstream/master`. The `nopsram` build **compiles clean with all four slices' first increments integrated** (`pio run -e nopsram`, verified 2026-07-10 — see `PLAN.md` Phase 0/2); flash-and-boot on real hardware is still unverified (no ESP32 board in this environment).

**Slice 1 (MMU + memory core) has a first real increment landed** — see `PLAN.md` Phase 2 for the full breakdown. In short: [`include/SCLD.h`](include/SCLD.h) + [`src/SCLD.cpp`](src/SCLD.cpp) now allocate and resolve real HOME ROM/RAM/DOCK/EXROM backing store (not stubs); `src/CPU.cpp` has a new `Z80Ops::is2068` flag and six `_2068` peek/poke/fetchOpcode/addressOnBus functions wired into the existing `_std`/`_2A3` function-pointer dispatch (**not** in-place edits to `_std`, which stays shared by 48K/128K/Pentagon/TK); `src/Ports.cpp` fully-decodes ports 0xF4/0xFF ahead of the Spectrum's partial-decode chain; `include/cpuESP.h` has sourced (libspectrum) 60Hz timing constants. A standalone host-side test, [`test/host/scld_test.cpp`](test/host/scld_test.cpp), exercises the real `SCLD.cpp` (30/30 checks passing) without needing ESP-IDF or hardware.

**Slice 4 (DOCK corpus + test harness) also has a first increment**: [`test/dock/`](test/dock/) has offline Python tooling for the `.DCK` cartridge container format (`dck_format.py` parser/writer, `dck_inspect.py` CLI, `make_synthetic_dck.py` fixture generator) plus a written provenance policy — no real cartridge/ROM dumps belong in this public repo; see `test/dock/README.md` before adding any binary image anywhere in the tree.

**Slice 3 (cartridge loader) has a first real increment, plus a second increment that found and fixed a real bug**: [`include/DockLoader.h`](include/DockLoader.h) + [`src/DockLoader.cpp`](src/DockLoader.cpp) parse `.DCK` files (a direct C++ port of `test/dock/dck_format.py`'s already-verified logic) and populate `SCLD`'s DOCK/EXROM chunks. Autostart deliberately does **not** use the `check_trdos` PC-watching pattern the original plan doc proposed — LROS only autostarts at boot, unlike TR-DOS, so a one-shot apply at reset is enough. Wired into `CPU::reset()`'s `"2068"` branch.

**The bug**: the first increment assumed an LROS cartridge could just be paged in and left to execute from `PC=0` (Z80::reset()'s default). Testing against a real LROS cartridge (`Zebra OS-64.BIN`, from the local reference library) proved that wrong — its real header occupies bytes 0-4 of chunk 0 as metadata, and the actual entry point is wherever the header's own start-address field points (`$0005` for Zebra, not `$0000`; landing at PC=0 would execute the header bytes as garbage instructions). Fixed: `DockLoader::autostartEntryPoint()` reads that field for real; `CPU::reset()` calls `Z80::setRegPC()` with it. [`test/host/real_cartridge_test.cpp`](test/host/real_cartridge_test.cpp) (18/18 checks) is what caught this — it wraps two **genuine** cartridge dumps (Zebra OS-64, a real LROS; `eToolkit.ROM`, a real AROS that correctly does *not* trigger autostart since it has no chunk 0) as in-memory `.DCK` buffers and runs them through the real loader, never touching the repo with the actual ROM bytes. `test/host/dockloader_test.cpp` (16/16) still covers the synthetic fixture and mechanical/error-handling cases; the real-content test is what a purely-synthetic fixture couldn't have caught, since its header bytes were never meant to resemble genuine cartridge metadata.

**Slice 2 (hi-res renderer + VGA mode table) now actually draws something, across two increments**: [`include/SCLDVideo.h`](include/SCLDVideo.h) + [`src/SCLDVideo.cpp`](src/SCLDVideo.cpp) implement and test the byte-to-pixel expansion math (standard-mode pixel-doubling, hi-res dual-display-file interleave, ink/paper decode — `test/host/scldvideo_test.cpp`, 15/15 checks). `src/ESP32Lib/VGA/VGA.h` has a new fixed 512-active-column `vidmodes[]` row (`SCLD_VGA_MODE_INDEX`), computed with the real APLL clock-divider algorithm from `tools/VideoModeTool.py`, wired into `Video.cpp`'s mode-select.

The second increment wired that math into an actual renderer, but **not** the way the first increment expected: `VIDEO::Draw`/`Draw_Opcode` are self-retargeting function pointers the existing Spectrum renderers reassign to each other as `tstates` crosses scanline boundaries (cycle-accurate rendering under memory contention) — TS2068 has no contention, so instead of building a TS2068 version of that state machine, [`VIDEO::RenderFrame2068()`](src/Video.cpp) renders the whole frame in one batch, called once per frame from `CPU::loop()`'s existing per-frame hook (`if (Z80Ops::is2068) VIDEO::RenderFrame2068(); else VIDEO::EndFrame();` — same call site every machine already used). `Draw`/`Draw_Opcode` stay pointed at the existing `&VIDEO::Blank` the whole session. See `PLAN.md`'s slice 2 writeup for what this covers (standard + hi-res modes, FLASH, BRIGHT) and doesn't (dual-file and hi-color modes render as standard-from-DFILE1, wrong but not blank; no border-color changes; no OSD compositing over a running TS2068 screen) — still entirely unverified on a real monitor, same as everything else in this repo.

**A local TS2068 reference library significantly upgraded source quality partway through this project**: `/Users/david/Documents/Projects/TS2068 Ref Library/` (local to this machine — not part of this repo, other contributors/sessions elsewhere won't have it; check it's still there before relying on a claim sourced from it). Real ROM disassemblies (HOME ROM + EXROM) and cross-referenced documentation (`docs/*.md` — memory map, video modes, dispatcher, I/O ports, errata). It already caught one real error: v0.1 of the plan doc had the hi-res video-mode bit value wrong (see `TS2068-ESPECTRUM-PORT-PLAN.md`'s "Port 0xFF" section) — though a later pass against FUSE (below) found that correction was itself incomplete; see PLAN.md's FUSE cross-reference writeup. **Contains real TS2068 ROM images** (`2068 ROMS/2068Home.BIN` + `2068Exrom.BIN` confirmed genuine — the disassembly-documented `$FF` version byte at HOME ROM offset `$0013` matches the real file — plus several alternative/homebrew ROMs like `gus-home.rom`/`gus-exrom.rom`, `eToolkit.ROM`, `Zebra OS-64.BIN`). **Provenance policy revised (2026-07-11), with a citable source, not just an assumption**: Timex's own stock TS2068 ROMs (`2068Home.BIN`/`2068Exrom.BIN`, and this project's own patched derivatives of them) are public domain, per a direct, on-file statement from Louis M. Galie (Senior VP, Timex Group USA Inc.) — full citation and quote in `test/dock/README.md`'s "Provenance policy" section — so those two files, specifically, are fine to copy into this repo. **This does not extend to the alternative/homebrew ROMs or the cartridge dump** (`gus-home.rom`/`gus-exrom.rom` are Gustavo Pane's own work, `eToolkit.ROM`/`Zebra OS-64.BIN` are third-party or unclear authorship) — those stay under the original conservative policy, never copied in without confirming redistribution rights first. `RomLoader` (below) reads all of them from this path directly at test time; nothing beyond the two Timex-original files should ever get copied in.

**A local FUSE 1.9.0 checkout (`/Users/david/Downloads/fuse-1.9.0`, also local to this machine, not part of this repo) is the highest-quality reference source found so far** — a mature, real, GPL, actively-TS2068-emulating codebase, not a summarized doc. `peripherals/scld.c`/`scld.h` (SCLD memory/video register emulation), `peripherals/dck.c` (`.DCK` loading), `machines/ts2068.c`/`tc2068.c`, `display.c`/`display.h` (rendering), `ui/fb/fbdisplay.c` (`uidisplay_plot16` — the exact pixel-expansion reference). A single cross-reference pass against it (2026-07-10) found **four real bugs already shipped in this repo** — see PLAN.md's "Cross-referencing FUSE" writeup for the full story: a port-address-matching bug in `Ports.cpp` that would have broken the MMU port for almost all real software, a hi-res pixel-interleave-order bug, an imprecise hi-res-submode trigger, and a standard-mode attribute-row-addressing bug that had been sitting untested inline in `Video.cpp`. All fixed and re-tested. When in doubt about SCLD/video/cartridge behavior going forward, check FUSE before trusting a summarized doc, including this project's own prior conclusions.

**ROM loading is real now**: [`include/RomLoader.h`](include/RomLoader.h) + [`src/RomLoader.cpp`](src/RomLoader.cpp) load a raw HOME ROM (16K) or EXROM (8K) binary from a file into `SCLD`'s backing store (`SCLD::loadHomeRom()` is new; EXROM reuses the existing `SCLD::loadExromImage()`). Deliberately does **not** reuse ESPectrum's existing Spectrum-machine "custom ROM" mechanism (`OSDMain.cpp`'s `updateROM()`) — that's a self-reflashing OTA-partition trick, confirmed via investigation to be too Spectrum-specific to be worth adapting; this is the much simpler "read a file, copy the bytes" shape instead. Wired into `ESPectrum.cpp`'s boot sequence for `Config::arch == "2068"`, from fixed conventional SD paths (`RomLoader::DEFAULT_HOME_ROM_PATH`/`DEFAULT_EXROM_PATH` — a bootstrapping convention, not a final OSD-integrated interface; that's still Phase 3). Host test (`test/host/romloader_test.cpp`, 13/13 checks) validates against the *real* ROM files above — confirms the documented `$FF` version-byte fact against actual loaded content, not just that a copy succeeded — and skips (doesn't fail) that part gracefully if the reference library isn't present.

**Keyboard input (port 0xFE, read side) is wired now**: `src/Ports.cpp`'s `is2068` branch of `Ports::input()` gained a `case 0x00FE` that folds the standard 8x5 Spectrum-compatible key matrix — reusing `Ports::port[0..7]` verbatim, which `ESPectrum::processKeyboard()` already keeps live every frame regardless of `Config::arch`, so this needed no new key-state plumbing, only the row-select-by-upper-address-byte loop (copied from the existing ULA branch a few lines below it). Two differences from that Spectrum path, both confirmed against FUSE (`peripherals/ula.c`'s `machine->timex` branch) and the TS2068 Technical Manual (Sec. 2.1.13.2's port FEH table): the idle default is the fixed byte `0x5F` (bits 5 and 7 always read 0 — no Issue2/3 quirk on the SCLD), and this port is fully low-byte decoded like 0xF4/0xFF rather than the Spectrum ULA's single-bit (A0==0) partial decode — confirmed against FUSE's `ula_ports_full_decode` table (`machines_periph_timex()`). The write side of port 0xFE (border color, beeper, tape MIC-out) is deliberately **not** implemented yet — a separate concern (audio + border rendering, the latter explicitly still missing per the line below) from keyboard scanning. No new host test: `Ports.cpp` isn't part of the `ESP2068_HOST_TEST` host-testable set (too many ESP-IDF/`VIDEO`/`Tape` dependencies), and the row-select-fold algorithm itself is a verbatim copy of the already-shipped, already-correct Spectrum-side code, not new derived arithmetic — verified instead by a clean `nopsram` build plus a hand trace of the CAPS-SHIFT-alone case against FUSE's confirmed bit table.

**AY-3-8912 sound ports (0xF5/0xF6) are wired now**: `src/Ports.cpp`'s `is2068` switches gained `case 0x00F5`/`case 0x00F6` in both `input()` and `output()`, reusing the existing `AySound` engine verbatim (`AySound::selectedRegister`, `setRegisterData()`, `getRegisterData()`) exactly the way the 128K/+2A/Pentagon path already does a few hundred lines below — no new sound-generation code, only new port decode. Confirmed against FUSE (`machines/tc2068.c`'s `tc2068_ay_registerport_read`/`tc2068_ay_dataport_read`, `machines/machines_periph.c`'s `tc2068_ay_ports` table) and the TS2068 Technical Manual (Sec. 2.1.6, 2.1.13, 4.3): 0xF5 is the register-select port, 0xF6 is data read/write, both fully low-byte decoded like the other TS2068 ports; unlike the plain 128K AY chip (whose data port has no read handler at all), the TS2068's data port can read back any register including 14; and the select port (0xF5) specifically always reads `0xff` while register 14 is selected — `AySound::getRegisterData()` already implements the same I/O-direction-bit gating FUSE's generic read path does, so no new masking logic was needed, only the R14-on-the-select-port special case. A prerequisite fix landed alongside this: `ESPectrum::AY_emu` and the `samplesPerFrame`/`audioOverSampleDivider`/`audioAYDivider`/`audioSampleDivider` audio-mixing constants were never set for `Config::arch == "2068"` (a live divide-by-zero risk the moment a port write pumped the audio buffer) — new `ESP_AUDIO_*_2068` constants (`include/ESPectrum.h`) were derived from `TSTATES_PER_FRAME_2068` (58688, already established in `cpuESP.h`) landing on an exact `31500`Hz output rate, same "perfect division" shape as the existing 48K/Pentagon rows, and `AY_emu = true` unconditionally (matching 128K/+2A/Pentagon — real TS2068 hardware always has the AY chip present, it's not a user-optional flag the way 48K/TK's `Config::AY48` is). **Deliberately not implemented**: joystick-via-AY-register-14 (real TS2068 hardware ANDs inverted joystick state into the data-port read via address bits 8/9 when R14 is selected — there's no Timex-joystick input source anywhere in this codebase yet, and omitting the fold is exactly equivalent to "no joystick connected," not an approximation of it, so nothing was faked). No new host test, same reasoning as the keyboard port: `Ports.cpp` isn't part of the `ESP2068_HOST_TEST` set, and this reuses already-shipped `AySound` logic rather than introducing new derived arithmetic — verified by a clean `nopsram` build instead.

**The TS 2040/ZX-Printer-compatible printer port (0xFB) is wired now**: unlike every other TS2068 port implemented so far, this one is genuinely new — there was no existing peripheral class anywhere in this codebase to reuse (confirmed by exhaustive search: zero printer-related code existed before this). [`include/SCLDPrinter.h`](include/SCLDPrinter.h) + [`src/SCLDPrinter.cpp`](src/SCLDPrinter.cpp) are a new, pure, host-testable class (no ESP-IDF dependency, same convention as `SCLDVideo`) implementing the real device's status/timing protocol: it's not a byte-in/byte-out port at all, but a synchronous stylus/motor interface where the "current print-head position" is derived purely from elapsed CPU cycles since the motor started (confirmed against FUSE's `peripherals/printer.c`, `printer_zxp_read`/`printer_zxp_write`, and the TS2068 Technical Manual Sec. 2.1.13.3/4.1.3's exact bit tables). Getting this right matters because real HOME ROM printer routines (`PRSCAN`/`K_DUMP`, Technical Manual Sec. 4.1.3) poll the status byte to pace themselves — wrong timing math means a hang, not just a visual glitch. Wired into `src/Ports.cpp`'s `is2068` switches as `case 0x00FB` (confirmed against FUSE's `PERIPH_TYPE_ZXPRINTER_FULL_DECODE` — TS2068 fully low-byte-decodes this port, unlike the 48K's partial "any address with bit 2 clear" decode — easy to conflate with the *other*, unrelated `PERIPH_TYPE_ZXPRINTER`/`PERIPH_TYPE_AY_TIMEX` FUSE tables, which are the 48K's own printer and the Spectrum SE's second AY chip respectively, not TS2068 hardware), passing `CPU::global_tstates + CPU::tstates` as the absolute cycle count — the same "whole-session absolute T-state" idiom `Tape.cpp` already uses for tape timing, kept `SCLDPrinter` itself framework-agnostic by design. `SCLDPrinter::reset()` is wired into `CPU::reset()`'s `"2068"` branch alongside the existing `SCLD::reset()`/autostart logic. **Deliberately not implemented**: persisting the printed image anywhere — `SCLDPrinter` tracks stylus/motor/head-position state precisely enough to answer "is the head busy, is it ready for the next pixel" correctly (the half real software depends on for correctness), but never accumulates pixel rows into a bitmap or writes anything to SD, unlike FUSE's own `printer_zxp_write()` (which does both). `test/host/scldprinter_test.cpp` (15/15 checks) covers idle/busy/ready-bit transitions, motor start/stop, `reset()` mid-print, and boundary safety (large time jumps, mid-print speed changes) precisely for the straightforward cases and as smoke/no-hang tests for the two genuinely intricate multi-line-crossing scenarios, where hand-deriving an exact expected byte was judged too failure-prone to trust blindly.

**Port 0xFE's write side (border color, beeper, tape-MIC-out) is wired now**: `src/Ports.cpp`'s `is2068` `output()` switch gained `case 0x00FE`, reusing the existing beeper-mixing formula and `VIDEO::borderColor`/`VIDEO::brd` state fields verbatim from the Spectrum ULA branch a few hundred lines below (same bit layout confirmed via the Technical Manual Sec. 2.1.13.2's write table: bits 2-0 border color, bit 3 tape/MIC out, bit 4 beeper, bits 5-7 unused) — the underlying audio pipeline was already arch-ready from the AY-port increment's `ESP_AUDIO_*_2068` constants, so this produces genuinely audible beeper output, not just a stub. Deliberately does **not** call `VIDEO::DrawBorder()` or the Pentagon/TK contention-timing `VIDEO::Draw()` calls the Spectrum branch uses alongside its own border write — those belong to the cycle-accurate, self-retargeting scanline renderer TS2068 never uses (`RenderFrame2068()` batches the whole frame instead; `Draw`/`Draw_Opcode` stay pointed at `&Blank` all session). `VIDEO::brdChange` is still set, confirmed harmless to do so: its only two consumers (`VIDEO::EndFrame()`, never called for `is2068`; the `MainScreen_Blank`-family `Draw_Opcode` functions, never retargeted away from `Blank` for `is2068` either) are both unreachable on this arch — so it's a dead flag here, kept updated only so a future border-rendering slice has correct state to read from day one.

**The expansion-bank port cluster (`$A0`/`$40`/`$80`/`$C0`/`$FC`/`$FD`) is scoped and deliberately not implemented — not an open gap.** These ports would have driven Timex's "Bus Expansion Unit" (BEU), a bank-switching accessory documented in the ROM's dispatcher (`GOTO_BANK`/`CALL_BANK`, `MAXBNK`) but, per the TS2068 Technical Manual's own port table, never commercially produced — two of the six ports are explicitly labeled `"Bank switching - not implemented"` in that table, and the other four don't appear in it at all. Corroborated three ways before deciding this: FUSE (25 years of active TS2068 emulation) registers zero ports for it and never mentions it anywhere; this project's own reference library states outright the BEU "was never tested against real hardware since [it] was never commercially produced"; and no real or modern software anywhere — including a currently-active 2023 homebrew storage project for this exact machine — exercises the mechanism. Net effect: this project's existing unhandled-port fallthrough (`0xFF` on read, discard on write, already true for every other unimplemented TS2068 port) is *already* period-accurate here, since it's indistinguishable from a real TS2068 with no BEU plugged in — which describes every TS2068 ever sold. `GOTO_BANK`/`CALL_BANK` themselves aren't BEU-exclusive (they're the OS's general bank-switch-and-jump primitive, also used for ordinary HOME/DOCK/EXT switches via the already-implemented `$F4`/`$FF` ports), so nothing mainstream is blocked by leaving this alone. See PLAN.md's writeup for the full evidence trail.

**Real bug found and fixed (2026-07-11): EXROM was wrongly modeled as a single 8K image mirrored across every chunk that selects it — real EXROM chip-select is per-chunk, exactly like DOCK's already-correct model.** This was a reasoned slice-1-era assumption (`SCLD.h`/`SCLD.cpp`'s own comment: *"chip-select gated by the SCLD, not by which Z80 chunk selected it... there's no larger EXROM address space... for a second bit to index into"*), made when only an 8K real EXROM dump was available to test against — mirrored-vs-chunk-indexed was genuinely indistinguishable from that evidence alone. Real hardware evidence surfaced while scoping the virtual-disk feature's EXROM sizing (see `TS2068-VIRTUAL-DISK-PLAN.md`) contradicted it: the project owner's real TS-Pico EXROM chip (`gus-exrom.rom`, in the local reference library) is genuinely 16384 bytes — double stock's 8192 — and its boot-time patch specifically activates EXROM in chunk 0 *and* chunk 1 together to reach *"the EXROM's extended code area"* (`gus-rom-analysis.md`), which would be pointless if both chunks showed an identical mirror. Fixed: `SCLD::exromPage()`/`loadExromImage()` now work exactly like `dockPage()`/`loadDockChunk()` — an 8-element chunk array, each chunk resolving to its own independent 8K slice (or the shared empty-socket page if that slice isn't loaded), with a new `loadExromChunk(int chunk, uint8_t*)`/`unloadExromChunk(int chunk)` pair mirroring DOCK's own per-chunk primitives (`DockLoader.cpp`'s `.DCK`-EXROM-bank loading already iterated per-chunk, so it had the same latent bug — each `BANK_EXROM` chunk was silently overwriting the previous one under the old single-pointer model). `RomLoader::loadExromFromBuffer()`/`loadExromFromFile()` now accept any whole multiple of 8K from 8K to 64K, not just exactly 8192 bytes. `test/host/scld_test.cpp` (35/35, +4) and `test/host/romloader_test.cpp` (21/21, +7, including a new check that loads the real `gus-exrom.rom` and empirically confirms chunk 0 ≠ chunk 1) both re-verified; all 7 host suites now total 143 checks. `nopsram` build clean.

**The virtual disk feature's ESP32-side infrastructure (Phase 4, Piece A) is wired now** — see `TS2068-VIRTUAL-DISK-PLAN.md`/`PLAN.md`'s "Phase 4" for the full design. [`include/VirtualDisk.h`](include/VirtualDisk.h) + [`src/VirtualDisk.cpp`](src/VirtualDisk.cpp) are a new, pure, host-testable class (same convention as `SCLDVideo`/`SCLDPrinter` — no dependency on `Z80.h`/`VIDEO`) implementing `.tap`-container mount/unmount, per-block read/write, a name-based block lookup this project's tape engine didn't previously expose as a queryable API, and a two-mode `CAT` (SD directory listing via plain `opendir()`/`readdir()`/`stat()`, and mounted-container block listing). `src/Ports.cpp`'s `is2068` switches gained `case 0x000E` (command, write)/`case 0x000F` (status, read) as the thin, non-host-tested adapter translating real Z80 registers (`Z80::getRegIX()`/`getRegDE()`/`getRegBC()`) and memory (`Z80Ops::peek8_2068()`/`poke8_2068()`, confirmed already callable from `Ports.cpp`'s existing includes) into calls on `VirtualDisk`'s pure API — buffers are sized dynamically (`std::vector`) from the real transfer length rather than capped at a fixed stack array, since a `CODE`/`SCREEN$` block can be many KB. `VirtualDisk::reset()` wired into `CPU::reset()`'s `"2068"` branch alongside `SCLDPrinter::reset()`. Two real protocol refinements surfaced during implementation, not present in the original design sketch: `MOUNT` split into `CMD_MOUNT_READ`/`CMD_MOUNT_WRITE` (keeps Piece A independent of the real `TADDR` system variable's encoding, which is genuinely the future EXROM patch's job to interpret, not this class's), and a `CMD_FIND_BLOCK` command added to fill the "name-based lookup" gap the design doc had already flagged but never assigned a wire-protocol slot. `Tape::Save()`'s `MemESP::readbyte()` call site is confirmed to have zero `is2068` awareness (a real bug had it been reused as originally sketched) — `VirtualDisk` doesn't call into `Tape::Save()` at all, instead reimplementing the same well-understood `.tap` block algorithm fresh, since reusing `Tape::Save()` would also entangle with `Tape`'s own separate global mount state. `test/host/virtualdisk_test.cpp` (50/50 checks, hand-built fixture independent of `VirtualDisk`'s own writer) brings all 8 host suites to 193 checks total. `nopsram` build clean. **Deliberately not implemented in this increment:** `.tzx` container mounting, non-`.tap` "direct load," and — the main point — none of this is reachable from real BASIC yet, since Pieces B/C (the EXROM/HOME ROM patches that would actually issue these port commands) haven't been written; everything here is verified via synthetic calls/fixtures only. **Follow-up fix, found while designing Piece C:** `CAT`'s protocol had no way to report a line's length — fixed by having `catSdLine()`/`catContainerLine()` set the high bit on each line's last byte as a terminator, the same self-delimiting convention real HOME ROM message tables already use, instead of needing a separate length channel.

**Piece B/C design substantially revised (2026-07-11), before any ROM assembly was written.** While starting implementation, a byte-run scan of the real `2068Exrom.BIN` (cross-checked against a labeled disassembly, `docs/Timex Sinclair 2068 EXROM.txt` in the local reference library) found `$1624`-`$1CFF` — over 1.5KB — is confirmed, disassembly-labeled dead space in stock EXROM, not the few dozen bytes originally assumed, and it sits entirely within chunk 0 (the same chunk `W_TAPE`/`R_TAPE`/`PGPSTR`'s patch points already live in). Confirmed with the project owner: **Piece B patches the real 8K EXROM in place — no 16K size extension, no per-intercept chunk-1 toggling.** `CAT`'s HOME ROM patch (`$25C8`, chunk 1) reaches the same free space via a real, live, RAM-resident TS2068 dispatcher routine, `BANK_ENABLE` (`$6499`) — confirmed from the disassembly and already used by real HOME ROM code immediately before `CAT`'s own dead stub — rather than hand-rolled port I/O; `CLOSE #4`'s own small intercept fits directly in a second confirmed-dead HOME ROM pocket (`$3CDC`-`$3CFF`, independently corroborated by `gus-rom-analysis.md` as the exact same pocket TS-Pico's real shipped ROM uses for its own trampoline).

**Piece D (real Z80 execution) landed the same day, via ZEsarUX/ZRCP — a real, independent emulator, not more of this project's own reasoning about itself.** [`patches/test_zesarux.py`](patches/test_zesarux.py) (17/17 checks, permanent and reusable) drives the assembled `patches/out/` binaries inside a live Z80 core over ZEsarUX's scriptable ZRCP protocol and confirms, live: the patched ROM boots completely through the real cold-start sequence to a genuine interactive BASIC ready prompt (the `K` cursor); ordinary BASIC still works (`PRINT 2+2` => `4`, a real regression check); `CLOSE4_intercept`'s logic in both branches (`STRMN==4` issues `CMD_UNMOUNT` and zeroes `BC`; otherwise `BC` survives completely untouched); and the full `CAT_trampoline` → `BANK_ENABLE` → `CAT_listing` chain, including confirming `BANK_ENABLE` (real ROM code, not this project's) genuinely pages the patched EXROM into chunk 0 (read back real EXROM bytes at `$0000` afterward to prove it, not assumed). **This testing found and fixed a real bug**: with no real `VirtualDisk` backend ever answering port `$0F`, `CAT_listing` had no way to detect `STATUS_EOF`/`STATUS_ERROR` and looped forever — confirmed live before the fix, fixed with a `DJNZ`-based `CAT_MAX_LINES` (200) bound, confirmed live after (both branch-taken and branch-not-taken cases). Still not confirmed, and can't be by more emulator scripting: the actual `D:`-prefixed command flow end-to-end, since ZEsarUX has no `VirtualDisk` backend for ports `$0E`/`$0F` — that needs real ESP32 hardware or a from-scratch host Z80 harness. Full account in `PLAN.md`'s Phase 4 Piece D writeup.

**Pieces B and C are now actually implemented, assembled, and hex-verified (2026-07-11)** — see [`patches/`](patches/) (`2068_exrom_patch.asm`, `2068_home_patch.asm`, `verify_patches.py`, `README.md`) and `PLAN.md`'s Phase 4 writeup for the full increment. All three items the previous paragraph left open resolved cleanly: real `SAVE`/`LOAD`'s own disassembly shows the caller `PUSH IX`/`CALL`/`POP IX`-wraps every `W_TAPE`/`R_TAPE` call rather than trusting any post-call register state, so the intercepts don't touch `IX` at all; the `"D:"` prefix check is a case-sensitive 2-byte compare that leaves `A`/`BC`/`DE` completely undisturbed on every non-match path; and the new system variables landed at `$5DDD` (`D_MODE_FLAG`, 1 byte) / `$5DDE` (`CAT_LINE_BUF`, 64 bytes) — no filename-pointer sysvar turned out to be needed at all, since `MOUNT` fires from inside the `PGPSTR` intercept while `DE`/`BC` already hold the right values. `patches/base/` now also holds the real, genuine stock `2068Home.BIN`/`2068Exrom.BIN` (copied into the repo — cleared specifically by the provenance resolution above) as the assembler's `INCBIN` source; `patches/out/` holds the assembled derivatives. `verify_patches.py`: 24/24 checks (every patch point's redirect bytes, every byte outside the five patch points and two new-code regions confirmed byte-identical to the real ROM including `CHRSET`, and spot-checks that the new code decodes as intended). **Said plainly, because it matters: nothing here has run against a real Z80 core, emulated or physical** — every byte is verified correct by hand-tracing against real disassembled ROM code plus the mechanical check above, not by watching `LOAD "D:test.tap"` actually work. That's Piece D, not started.

**Not yet done, so don't assume it works:** the OSD menu has no "2068" entry or file picker wired to `DockLoader::loadFromFile()`/`RomLoader`'s fixed paths (Phase 3), AROS autostart itself (only *not incorrectly triggering it* is verified), dual-file/hi-color video modes, mid-frame border-color *rendering* (the port write now latches `VIDEO::borderColor` correctly, but nothing draws it — `RenderFrame2068()` doesn't composite a border at all yet), Timex-joystick input (register-14 AY reads always report "no buttons pressed"), printed output isn't persisted anywhere (status/timing protocol only), the virtual disk feature's `D:`-prefixed command flow has never run end-to-end against a real `VirtualDisk` backend (the ROM patches themselves *have* been confirmed correct against a real, live Z80 core — see the Piece D paragraph above — but nothing has exercised `LOAD "D:..."`/`SAVE`/`CAT`/`CLOSE #4` for real, since that needs ESP32 hardware or a from-scratch host harness neither of which exist yet), `D:` `VERIFY` isn't a true non-destructive verify (behaves like `LOAD`), and non-`.tap`/`.tzx` virtual-disk files remain unsupported — and nothing has booted on real hardware — even with real ROM/cartridge content now loadable and verified against genuine dumps, and the virtual disk ROM patches now live-tested in ZEsarUX, everything so far is validated by host-side tests, hex-level and live ROM patch verification, and a clean `nopsram` compile, not by running real software on a real board.

## Upstream facts worth knowing before touching code

- **License: GPL-3.0.** ESPectrum is GPL-3.0-or-later. Anything derived from it — including this port — must stay GPL-3.0 compatible. Don't suggest closed-sourcing, relicensing, or pulling in incompatible-license dependencies.
- **Build system:** PlatformIO, `framework = espidf`, `platform = espressif32@5.4`, `board = pico32`. Two build envs in `platformio.ini`: `psram` (default) and `nopsram`. This port targets `nopsram` — the plan doc's memory budget assumes ~320K usable internal SRAM and explicitly does not need PSRAM.
- **Hardware:** ESP32 dev boards with VGA output (TTGo VGA32-class is the reference board), PS/2 keyboard input, SD card for file/cartridge loading. Same board class works for both Spectrum and TS2068 modes — no new hardware, just new firmware paths.
- **Where the port touches the tree:**
  - `src/CPU.cpp` — has real `Z80Ops::peek8_2068`/`poke8_2068`/`peek16_2068`/`poke16_2068`/`fetchOpcode_2068`/`addressOnBus_2068` now, mirroring `_std`'s shape (`>>13`/`&0x1fff` instead of `>>14`/`&0x3fff`) but as new sibling functions selected via the existing function-pointer dispatch — `_std` itself is untouched and still shared by 48K/128K/Pentagon/TK. (Not `include/Z80_JLS/z80.h` — that file only holds the opcode dispatch table and calls through function pointers defined in `z80operations.h`, implemented in `CPU.cpp`.)
  - `include/MemESP.h` / `src/MemESP.cpp` — **untouched, deliberately.** TS2068 does not use `MemESP::ramCurrent[4]`/`ram[8]`/`rom[5]` at all; it has its own backing store in `SCLD.cpp` (HOME 64K + EXROM 8K-64K, chunk-indexed like DOCK + DOCK ≤24K, allocated separately). This coexists with the Spectrum machines rather than replacing their memory model — matches how `Config::arch` already lets one firmware serve multiple machines at runtime.
  - `include/SCLD.h` / `src/SCLD.cpp` — the TS2068 memory model and port 0xF4/0xFF register contract. New files, no upstream equivalent.
  - `include/DockLoader.h` / `src/DockLoader.cpp` — `.DCK` cartridge parsing and LROS-style autostart. New files, no upstream equivalent. Both `SCLD.cpp` and `DockLoader.cpp` also compile host-side under `-DESP2068_HOST_TEST` (see their host tests in `test/host/`) — keep new code in this pair of files free of ESP-IDF-only headers where practical, so that stays true.
  - `include/RomLoader.h` / `src/RomLoader.cpp` — HOME ROM/EXROM loading from a raw binary file. New files, no upstream equivalent, same `-DESP2068_HOST_TEST` convention as `DockLoader.cpp`.
  - `include/SCLDVideo.h` / `src/SCLDVideo.cpp` — byte-to-pixel expansion math for standard/hi-res display modes. New files, no upstream equivalent, no ESP-IDF dependency at all (host-testable without any `-D` flag). Called from `VIDEO::RenderFrame2068()` (`src/Video.cpp`) — see the slice 2 notes above.
  - `src/ESP32Lib/VGA/VGA.h` — has one new `vidmodes[]` row (`SCLD_VGA_MODE_INDEX`) for TS2068's fixed 512-column mode, appended after the existing CRT modes. This is vendored third-party code (`ESP32Lib`, from bitluni); the addition follows its existing pattern (a macro + one array entry) rather than restructuring it.
  - `src/Video.cpp` — `VIDEO::Init()`/`VIDEO::vgataskinit()` special-case `Config::arch == "2068"` to force `SCLD_VGA_MODE_INDEX`, bypassing the Spectrum/TK/Pentagon aspect-ratio/CRT mode-select logic. `VIDEO::Reset()` has an `is2068` branch that sets `VsyncTarget` (frame-pacing, matters for every arch) and returns early, skipping the Spectrum-only border/OSD-position setup that doesn't apply. New method `VIDEO::RenderFrame2068()` does the actual once-per-frame batch render — see the slice 2 notes above. `VIDEO::Draw`/`Draw_Opcode` themselves stay pointed at the existing `&Blank` for TS2068, never retargeted.
  - `src/CPU.cpp`'s `CPU::loop()` — one new branch at the existing per-frame `VIDEO::EndFrame()` call site: `if (Z80Ops::is2068) VIDEO::RenderFrame2068(); else VIDEO::EndFrame();`.
  - `src/Video.cpp` / `ESP32Lib/VGA/VGA.h` / `VGA6Bit.h` — display renderer and the `vidmodes[][17]` VGA timing table. `vga.init()` (which sizes the framebuffer and DMA descriptors) is called exactly once per boot, selected by `Config::arch[0]`; there is no live resolution switch anywhere in ESPectrum. The TS2068 port's resolved design is to boot into one fixed 512-column physical mode always and let the SCLD's port 0xFF video-mode bits pick which renderer fills it (see plan doc's "Display" section) — this avoids needing to invent live mode-switching.
  - `src/Ports.cpp` — I/O port dispatch. Existing Spectrum ports use partial address decoding (e.g. `(address & 0x8002) == 0`); the 2068's ports 0xFF and 0xF4 are fully decoded and the OUT handlers must match the full address, not a bitmask subset.
  - `src/Z80_JLS.cpp` `Z80::check_trdos()` — the "watch PC high byte, repoint a bank pointer, flip a flag" pattern used for TR-DOS autostart. This is the template for LROS/AROS/DOCK cartridge autostart (plan doc, Cartridge loading section).
  - `src/ESPConfig.cpp` (declares `class Config` in `include/ESPConfig.h` — file is `ESPConfig`, not `Config`) / `src/OSDMain.cpp` — machine-selection menu and `Config::arch` string (`"48K"`, `"128K"`, `"+2A"`, `"Pentagon"`, `"TK90X"`, `"TK95"`). A TS2068 entry adds a new arch string and a first-character case in the switches that key off `Config::arch[0]` (e.g. in `Video.cpp`'s mode-select). No existing first character collides with a `2068`-style prefix.

## Conventions for working in this repo

- The plan doc (`TS2068-ESPECTRUM-PORT-PLAN.md`) is the source of truth for the SCLD register contract and memory model. If an implementation detail there turns out to be wrong once code exists, update the doc in the same change — don't let it drift into a stale spec.
- Prefer small, slice-scoped changes that match the four-slice breakdown (MMU/memory core, hi-res renderer + VGA timing, cartridge loader, DOCK/test corpus) over sweeping refactors — the whole point of that split is that the slices share almost no code surface.
- This is embedded C/C++ on a resource-constrained target (no PSRAM in the primary build). Be deliberate about anything that grows static footprint or adds per-access branches on the Z80 peek/poke hot path — the plan doc calls out that the MMU port should be a shift/mask constant change, not a new branch.
- Don't invent write-protect or contention mechanisms that don't exist upstream — ESPectrum has no generalized per-page read-only flag (ROM protection there is a single hardcoded `page == 0` check). This port has its own now: `SCLD::memChunkReadOnly[8]`, gated in `CPU.cpp`'s `poke8_2068`/`poke16_2068`. Use that, don't reinvent it.
